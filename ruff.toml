# Keep the repository-wide baseline aligned with the two pipeline projects.
target-version = "py310"
line-length = 88

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[lint.isort]
combine-as-imports = true
force-sort-within-sections = true
order-by-type = true
